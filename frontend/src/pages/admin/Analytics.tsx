import React from 'react'; import PageShell from '../../components/PageShell'; import { adminAPI } from '../../services/api'; export default function Analytics() { const [stats, setStats] = React.useState<Record<string, number> | null>(null); React.useEffect(() => { adminAPI.getStats().then(({ data }) => setStats(data.stats || data)).catch(() => undefined); }, []); return <PageShell title="Analytics"><pre className="rounded-xl bg-white p-6 shadow">{JSON.stringify(stats || {}, null, 2)}</pre></PageShell>; }
